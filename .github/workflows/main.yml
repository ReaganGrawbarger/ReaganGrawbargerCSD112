# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages
on:
 # Runs on pushes targeting the default branch
 push:
 branches: ["main"]
 # Allows you to run this workflow manually from the Actions tab
 workflow_dispatch:
# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
 contents: read
 pages: write
 id-token: write
# Allow only one concurrent deployment, skipping runs queued between the run
in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these
production deployments to complete.
concurrency:
 group: "pages"
 cancel-in-progress: false
jobs:
 # Single deploy job since we're just deploying
 deploy:
 environment:
 name: github-pages
 url: ${{ steps.deployment.outputs.page_url }}
 runs-on: ubuntu-latest
 steps:
Based on the path provided to the upload artifact we need to also create an index file at the root
of the site .
This index.html file should include the following:
Within the site directory we need to provide some core scaffolding for CSS and JS as well. To
accomplish this we need to create 2 empty files:
At this point there we should be ready to commit our work to the new branch lab-1 .
Add the screen shot taken of the extensions installed in section 2 to the directory:
supplimental/lab-1/ and commit it as well.
 - name: Checkout
 uses: actions/checkout@v4
 - name: Setup Pages
 uses: actions/configure-pages@v5
 - name: Upload artifact
 uses: actions/upload-pages-artifact@v3
 with:
 # Upload site directory
 path: 'site'
 - name: Deploy to GitHub Pages
 id: deployment
 uses: actions/deploy-pages@v4
